"""
Admin configuration for CV extraction app.
"""
from django.contrib import admin

# CV extraction models are stored in MongoDB, not Django models
# Admin functionality is handled through views

